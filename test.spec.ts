import { describe, expect, test } from '@jest/globals';
import axios from 'axios';
describe('Full test', () => {
  test('register', () => {
    expect(3).toBe(3);
  });

  test('login', () => {
    expect(3).toBe(3);
  });

  test('deposit with out login', () => {
    expect(3).toBe(3);
  });

  test('deposit', () => {
    expect(3).toBe(3);
  });

  test('withdraw', () => {
    expect(3).toBe(3);
  });

  test('withdraw over balance', () => {
    expect(3).toBe(3);
  });

  test('get user balance', () => {
    expect(3).toBe(3);
  });

  test('get user balance by id', () => {
    expect(3).toBe(3);
  });
});
